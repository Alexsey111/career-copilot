# Application Lifecycle

The application lifecycle is deterministic and status-driven.

Core stages:

- draft
- ready
- applied
- screening
- interview
- offer
- rejected
- withdrawn

The application pipeline keeps timeline and activity log events separate from the current status so the UI can show both the present state and the history.

Any demo or pilot flow should keep submission human-driven and avoid implying automatic external submission.
